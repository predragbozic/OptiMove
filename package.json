{
  "name": "optimove-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend ci",
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev"
  }
}
