{
  "name": "optimove-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/**/*.test.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "vite": "^8.2.1"
  }
}
